{
  "version": "2026.07.24-mobile-browser-fix-v20",
  "context": "User reported (with a screenshot from a real Galaxy Note 20 Ultra, Edge browser) that the character-select screen's start button was clipped on initial load in portrait (fixed by physically rotating the device), vertical movement didn't work well in Edge specifically (worked fine in Chrome), a native copy/selection popup appeared during held-down dpad presses, and asked for a general bug-review pass.",

  "fixes": {
    "clipped_start_button_on_initial_load": {
      "status": "done",
      "root_cause_found_via_real_browser_testing": "Two separate bugs stacked on top of each other, both only found by measuring actual computed styles/bounding boxes in a headless-Chromium run, not by code review alone: (1) the portrait auto-rotate CSS used raw vh/dvh units for #gameShell's size, and dvh support is inconsistent across Android WebView variants (Samsung Internet/Edge lag behind Chrome) -- a real device's dvh could be momentarily wrong on first paint before the address bar settles, matching 'fixed by rotating the device' (which forces a resize/orientationchange recompute). (2) MUCH more impactful: the character-select screen's own CSS (.select-content, .character-card, .load-track, .modal-card, and two whole responsive breakpoints) used raw vw/vh units and orientation:landscape media queries. Both of these always reference the PHYSICAL viewport/orientation, which is *wrong* once #gameShell is CSS-rotated 90deg -- a portrait phone is still physically 'portrait' and narrow, so vw-based sizing and the orientation:landscape breakpoints silently never applied at all, rendering the select screen at full desktop-ish size squeezed into a rotated portrait frame. This was the dominant cause of the clipping in the screenshot, not just viewport-settle timing.",
      "fix": "game.js's resize() now exposes the game's logical (post-rotation-swap) width/height as --game-w/--game-h CSS custom properties, plus toggles compact-w/compact-h classes on <html> based on those logical dimensions. style.css was updated to size .select-content/.character-card/.load-track/.modal-card off --game-w/--game-h instead of vw/vh, and the two broken 'orientation:landscape' breakpoints were rewritten as html.compact-w/.compact-h class selectors so they correctly apply based on logical size regardless of physical device orientation. resize() is also now re-fired a few times over the first second after load (rAF + 150/400/900ms) to catch any late browser-chrome settling.",
      "verified_with_real_browser": "Simulated a Note 20 Ultra-class portrait viewport (412x915). Before the fix: #startBtn measured at x=-148 (fully off-screen to the left). After the fix: #startBtn, .select-content, .character-cards, and all three .character-card elements measured fully within the (0,0)-(412,915) physical viewport bounds, both immediately after load (0.3s) and after settling (1.5s). Also confirmed compact-h correctly activates for this viewport's logical 412(w)x412(h)-ish landscape dimensions, and that a normal 1200x700 desktop landscape viewport gets neither compact class (no unwanted shrinking on large screens)."
    },

    "vertical_movement_not_working_in_edge": {
      "status": "best-effort fix applied, exact root cause not confirmed (no access to a real Edge/Samsung Internet browser in this environment)",
      "most_likely_cause": "The native long-press text-selection/copy popup (see next item) stealing the touch/pointer capture mid-hold. If this happened more readily on the up/down buttons than left/right for any reason (e.g. button layout, timing), it would look exactly like 'horizontal works, vertical doesn't' even though the actual bug is direction-agnostic.",
      "fixes_applied": [
        "Disabled text selection, long-press callout, and tap-highlight globally via CSS (-webkit-user-select/-webkit-touch-callout/-webkit-tap-highlight-color), plus explicitly on #dpad and its children.",
        "Added JS-level contextmenu/selectstart/copy event blocking as defense-in-depth, since some Android WebView browsers can still surface a native menu in edge cases CSS alone doesn't catch.",
        "Added touchstart/touchend/touchcancel listeners alongside the existing pointerdown/up/cancel on every dpad button, since Samsung Internet/Edge-on-Android have historically had less consistent Pointer Events support than desktop-parity Chrome -- this directly matches a 'works in Chrome, not Edge' symptom pattern. The two event systems firing together is harmless since all the handlers are idempotent (setting the same boolean/class twice)."
      ],
      "not_verified": "Could not reproduce or test on an actual Edge-for-Android or Samsung Internet browser in this environment. If vertical movement is still unreliable in Edge after this update, please note whether it happens with the on-screen dpad, a keyboard (if testing via desktop Edge), or both -- that would narrow this down further."
    },

    "copy_popup_during_held_dpad_press": {
      "status": "done",
      "root_cause": "No text-selection/long-press-callout prevention existed anywhere in the CSS or JS.",
      "fix": "Same fix as above (global -webkit-user-select:none/-webkit-touch-callout:none plus JS contextmenu/selectstart/copy blocking). This should stop the native copy/share popup from appearing during a held-down directional press."
    },

    "general_code_review": {
      "status": "done",
      "method": "Ran the full JS syntax check, JSON validity check, road-graph integrity check, and the AUTO-navigation movement simulation -- all pass. Additionally ran a real headless-browser playthrough (character select -> start -> move in all 4 directions -> AUTO start/cancel -> interact -> open/close every HUD panel: ranking, codex, settings, menu, quest collapse) while capturing all console errors, warnings, and uncaught page errors -- zero were raised across either a desktop-landscape viewport or the Note-20-Ultra-class portrait viewport.",
      "scope_note": "This was a behavioral/runtime pass (does it actually run cleanly end to end), not a line-by-line audit of every file in the repository -- the codebase is sizeable (386-line game.js plus ~10 src/ modules, Cloudflare Functions, etc.) and a full manual audit of all of it was outside what could be responsibly completed in this session. The rotation/viewport-unit bug above was the significant bug this pass surfaced; nothing else raised errors or looked obviously wrong in the paths exercised."
    }
  },

  "regression_suite_rerun": {
    "js_syntax_check": "pass, 0 failures",
    "json_validity": "pass, all files including manifest.webmanifest",
    "road_graph_integrity": "pass, 27 nodes / 35 edges / 7 hotspots, fully connected",
    "auto_navigation_simulation": "pass, all 7 hotspots reached, 0.000 final distance, no NaNs/stalls",
    "headless_browser_playthrough": "pass, 0 console errors/warnings/pageerrors across desktop-landscape and Note-20-Ultra-portrait viewports",
    "portrait_clipping_regression_test": "pass, all character-select elements measured fully within physical viewport bounds (previously #startBtn was off-screen at x=-148)"
  },

  "deployment": {
    "cloudflare_pages": {
      "framework_preset": "None",
      "build_command": "(empty)",
      "build_output_directory": "/",
      "d1_binding": "DB"
    }
  }
}
